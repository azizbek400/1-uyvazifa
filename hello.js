// let str = "tewst";

// export const hello = (a) => {
//     if (a.length % 2 == 0) {
//         let b = a.length / 2 - 1;
//         let c = b + 1;
//         return a[b] + a[c];
//     }

//     return a[Math.floor(a.length / 2)];
// };

// console.log(hello(str));